<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wuju.gmall.gmallmanageservice.mapper.SkuSaleValueMapper">
    <resultMap id="skuSaleValueMapper" type="com.wuju.gmall.SkuSaleAttrValue" autoMapping="true">
    <id column="id" property="id"></id>
    </resultMap>
    <select id="selectSkuSaleAttrValueListBySpu" resultMap="skuSaleValueMapper">
        SELECT
	sale_attr_value_id,
	sku_id,
	sale_attr_value_name
FROM
	sku_sale_attr_value ssav,
	sku_info si
WHERE
	ssav.sku_id = si.id
AND si.spu_id = #{0}
ORDER BY si.id ,ssav.sale_attr_id
    </select>
</mapper>